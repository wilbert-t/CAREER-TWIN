providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["pip install -r career-twin/backend/requirements.txt"]

[start]
cmd = "cd career-twin/backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
