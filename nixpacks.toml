providers = ["python"]

[phases.install]
cmds = ["python3 -m pip install -r career-twin/backend/requirements.txt"]

[start]
cmd = "cd career-twin/backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
